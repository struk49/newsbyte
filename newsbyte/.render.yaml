services:
  - type: web
    name: newsbyte
    runtime: python
    buildCommand: pip install -r newsbyte/requirements.txt && cd newsbyte && python manage.py collectstatic --noinput
    startCommand: cd newsbyte && gunicorn newsbyte.wsgi:application
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: newsbyte.settings
      - key: PYTHON_VERSION
        value: 3.11
    disk:
      name: media
      mountPath: /mnt/data
